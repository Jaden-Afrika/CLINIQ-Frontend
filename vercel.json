{
  "version": 2,
  "installCommand": "cd cliniq-frontend && npm install",
  "buildCommand": "cd cliniq-frontend && npm run build",
  "outputDirectory": "cliniq-frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
